{
  "name": "hepi-listings",
  "version": "1.0.0",
  "private": true,
  "description": "HEPI Property Listings — Node.js Fastify website (Google Sheets + Drive)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.1",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "googleapis": "^144.0.0"
  }
}
